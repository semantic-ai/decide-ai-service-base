[project]
name = "decide-ai-service-base"
version = "0.3.0"
description = "Base package for implementing AI services in the template for the DECIDe project"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.12.5",
    "fastapi==0.121.1",
    "PyYAML==6.0.3",
    "pydantic-settings>=2.0.0",
]
readme = "README.md"
